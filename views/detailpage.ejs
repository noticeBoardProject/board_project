<!DOCTYPE html>
<html lang="en">

<head>
  <link rel="stylesheet" href="/public/CSS/detailpage.css" />
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js">
  </script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>상세페이지</title>
</head>

<body>
  <!-- 헤더 -->
  <%- include("basicHeader") -%>
    <div class="main-wrap">
      <main>
        <div class="board-info-wrap">
          <div class="category">
            <%= data[0].category %>
          </div>
          <div class="title">
            <%= data[0].title %>
          </div>
          <div class="board-info-box">
            <div class="board-info">
              <div class="nickname">
                <%= data[0].nickname %>
              </div>
              <span class="day">
                <%= data[0].updateAt %>
              </span>
              <span class="likecount">
                <%= data[0].likeCount %>
              </span>
            </div>
            <div class="mybtn">
              <button>수정</button>
              <button>삭제</button>
            </div>
          </div>
        </div>
        <div class="imgbox">
          <% data[0].img_url.forEach((image,i)=>{ %>
            <img src="<%= image[i] %>" alt="등록된 이미지" />
            <% }) %>
        </div>

        <div>내용</div>
        <div class="likebox">
          <img src="/public/image/binheart.svg" alt="bin" />
          <span>좋아요</span>
        </div>
      </main>
    </div>
</body>
<script src="/public/JS/detailpage.js"></script>

</html>